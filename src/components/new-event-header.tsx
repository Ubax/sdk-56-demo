// Fallback for Android/web — the custom glass header is iOS-only, so other
// platforms fall back to the default native stack header.
export const NEW_EVENT_HEADER_HEIGHT = 0;

export function NewEventHeader() {
  return null;
}
